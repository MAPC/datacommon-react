export const unformattedColumns = [
  // year columns:
  "fy_year", "fiscal_yr", "fy", "acs_year", "year", "dec_year", "cal_year", "schoolyear",
  // census tract columns:
  "ct10_id", "ct20_id", "geoid", "logrecno", 
  // block group columns:
  "bg10_id", "bg20_id", 
  // blocks columns:
  "blk10_id", "blk20_id",
  // other specific columns:
  "schid",
]